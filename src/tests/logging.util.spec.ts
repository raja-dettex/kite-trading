import { writeLog, readLog} from './../utils/logging.util'

describe("test suite for commit log" , () => {
    it("should do well" , () => { 
        for( let i = 0; i < 5; i++) { 
            //writeLog("abc ")
        }
    })

    it("shoud read all " , ()=> { 
        //readLog()
    })
})